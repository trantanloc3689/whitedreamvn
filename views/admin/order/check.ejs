<% include ../layout/header.ejs %> 

<div id="page-wrapper">
    <div class="row">
        <div class="col-lg-8">
            <h1 class="page-header"> Danh sách đơn hàng</h1>
        </div>
    </div>
    <!-- /.row -->
    <div class="row">
        <% if(success_msg != ''){ %> 
            <div class="alert alert-success"><%= success_msg%></div>
        <% } %>
        <% if(error_msg1='') { %>
            <div class="alert alert-danger"><%= error_msg %>  </div>
        <% };%>
        <!-- /.col-lg-6 -->
        
        <div class="col-lg-12">
                    <ul class="nav nav-tabs">
                        <li role="presentation"><a href="/admin/order">Tất cả</a></li>
                        <li role="presentation"><a href="/admin/order/checked">Đã duyệt</a></li>
                        <li role="presentation" class="active"><a href="/admin/order/check">Chưa duyệt</a></li>
                    </ul>

            <div class="panel panel-default">
                <div class="panel-body">
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th>STT</th>
                                    <th> Người Mua hàng</th>
                                    <th> SDT</th>
                                    <th> Địa Chỉ</th>
                                    <th> Tổng giá tiền</th>
                                    <th> Trạng thái</th>
                                    <th>Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                
                            </tbody>
                        </table>
                    </div>
                    <!-- /.table-responsive -->
                </div>
                <!-- /.panel-body -->
            </div>
            <!-- /.panel -->
        </div>
        <!-- /.col-lg-6 -->
    </div>
</div>
<!-- /#page-wrapper -->
<!-- Button trigger modal -->


<% include ../layout/footer.ejs %> 